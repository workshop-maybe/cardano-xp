# =============================================================================
# Andamio T3 App Template - Environment Configuration
# =============================================================================
#
# Copy this file to .env and fill in the values.
# Never commit .env to version control!
#
# When adding new variables, update the schema in /src/env.js
#
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: Unified Andamio API Gateway
# -----------------------------------------------------------------------------
# The unified gateway combines all Andamio APIs:
# - Authentication and user management
# - Course and project data (merged off-chain + on-chain)
# - Andamioscan on-chain indexed data (/v2/*)
# - Transaction building (/v2/tx/*)
#
# Preprod (default for development):
NEXT_PUBLIC_ANDAMIO_GATEWAY_URL="https://preprod.api.andamio.io"

# -----------------------------------------------------------------------------
# REQUIRED: Andamio API Key (Server-side)
# -----------------------------------------------------------------------------
# API key for server-side requests to the Andamio Gateway.
# All API usage through this app is billed to this key's account.
#
# To generate an API key:
# 1. Connect wallet with Access Token at /api-setup
# 2. Register with Gateway (or login if already registered)
# 3. Generate API key with a name (e.g., "my-app-production")
#
# IMPORTANT: Keep this secret! Never expose in client-side code.
ANDAMIO_API_KEY="your-api-key-here"

# -----------------------------------------------------------------------------
# REQUIRED: Cardano Network Configuration
# -----------------------------------------------------------------------------
# Determines which Cardano network to use for wallet connections and
# transaction building. Must match your API configurations above.
#
# Options: "preprod" | "mainnet" | "preview"
NEXT_PUBLIC_CARDANO_NETWORK="preprod"

# -----------------------------------------------------------------------------
# REQUIRED: Andamio Access Token Policy ID
# -----------------------------------------------------------------------------
# The policy ID for Andamio Access Tokens on your target network.
# These tokens identify users on the blockchain.
#
# Environment | Policy ID                                                | App URL
# ------------|----------------------------------------------------------|------------------------
# Preprod     | aa1cbea2524d369768283d7c8300755880fd071194a347cf0a4e274f | preprod.app.andamio.io
# Mainnet     | ff5d0640b5a2717646d3f3151d100d57d194fdfa88cacf03f9edc568 | mainnet.app.andamio.io
#
# For local development, use the Dev policy ID:
NEXT_PUBLIC_ACCESS_TOKEN_POLICY_ID="aa1cbea2524d369768283d7c8300755880fd071194a347cf0a4e274f"

# -----------------------------------------------------------------------------
# REQUIRED: Cardano XP — Single Course and Project
# -----------------------------------------------------------------------------
# This app serves a single course and a single project.
# These IDs are public — they identify which on-chain course and project
# this instance of the app is built around.
#
# Set these after creating the course and project on Andamio.
NEXT_PUBLIC_COURSE_ID="your-course-id"
NEXT_PUBLIC_PROJECT_ID="your-project-id"

# =============================================================================
# OPTIONAL: Blockfrost Provider (for social wallet TX submission)
# =============================================================================
# Required for submitting transactions when logged in via social wallet
# (Google, Discord, X via UTXOS/Mesh Web3 Services).
# Not needed for CIP-30 browser wallets (Nami, Eternl, etc.).
#
# Get a free key at: https://blockfrost.io/
# For preprod, project IDs start with "preprod"
NEXT_PUBLIC_BLOCKFROST_PROJECT_ID="preprodXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

# =============================================================================
# OPTIONAL: Additional Configuration
# =============================================================================

# Debug mode - enables verbose logging in browser console
# NEXT_PUBLIC_DEBUG="true"

# Site URL — controls metadataBase for OG images and canonical URLs.
# Set to your production domain when deployed.
# NEXT_PUBLIC_SITE_URL="https://cardano-xp.io"

# Custom explorer URL (defaults to cardanoscan.io)
# NEXT_PUBLIC_EXPLORER_URL="https://preprod.cardanoscan.io"

# =============================================================================
# OPTIONAL: UTXOS Transaction Sponsorship (for gasless migration)
# =============================================================================
# Enables gasless V1→V2 access token migration. The developer wallet pays
# the transaction fee so migrating users don't need ADA.
#
# Setup:
# 1. Go to https://utxos.dev/dashboard
# 2. Create a project and copy the Project ID
# 3. Generate an API key for server-side use
# 4. Create a Sponsorship under your project
# 5. Fund the sponsorship wallet with ADA
# 6. Copy the Sponsorship ID below
#
# Client-side (baked into build):
NEXT_PUBLIC_WEB3_SDK_PROJECT_ID="your-utxos-project-id"

# Server-side (set in Cloud Run environment variables):
WEB3_SDK_API_KEY="your-utxos-api-key"
WEB3_SDK_PRIVATE_KEY="your-utxos-private-key"
UTXOS_SPONSORSHIP_ID="your-sponsorship-id"

# =============================================================================
# OPTIONAL: E2E Testing Configuration
# =============================================================================
# These variables are only needed for running E2E tests with real wallets.
# See e2e/README.md for full setup instructions.

# Blockfrost API key for real wallet tests (preprod network)
# Get a free key at: https://blockfrost.io/
# BLOCKFROST_PREPROD_API_KEY="preprodXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

# Custom test wallet mnemonic (24 words, space-separated)
# WARNING: Only use test wallets! Never use mainnet mnemonics!
# TEST_WALLET_MNEMONIC="word1 word2 word3 ... word24"
