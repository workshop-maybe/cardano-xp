import { AndamioStudioLoading } from "~/components/andamio/andamio-loading";

/**
 * Loading state for project studio detail.
 * Shows a centered workspace skeleton.
 */
export default function StudioProjectDetailLoading() {
  return <AndamioStudioLoading variant="centered" />;
}
